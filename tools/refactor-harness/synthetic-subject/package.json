{
  "name": "qg-synthetic-subject",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Ground-truth subject for the provider refactor. Deliberately broken in known, counted ways.",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.53.0",
    "@typescript-eslint/parser": "^8.53.0",
    "@vitest/coverage-istanbul": "^3.2.4",
    "eslint": "^9.18.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
